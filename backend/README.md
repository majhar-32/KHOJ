# Backend — Not Started Yet

This project is being built **frontend-first**. The backend (Node.js + Express + PostgreSQL via Prisma) has not been started.

See the root `PROJECT_LOG.md` for the current plan and progress.
